<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.community.repository.DynamicLikeRepository">
    <insert id="createDynamicLike" parameterType="com.spring.community.DTO.LikeSaveDTO">
        CREATE TABLE IF NOT EXISTS like_#{nickName} (
        like_id BIGINT PRIMARY KEY AUTO_INCREMENT,
        user_id BIGINT NOT NULL,
        post_id BIGINT NOT NULL,
        FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE,
        FOREIGN KEY (post_id) REFERENCES post(post_id) ON DELETE CASCADE
        );
    </insert>

    <insert id="insertDynamicLike" parameterType="com.spring.community.DTO.LikeSaveDTO">
        INSERT INTO like_#{nickName} (user_id, post_id)
        VALUES (#{userId}, #{postId});
    </insert>

</mapper>